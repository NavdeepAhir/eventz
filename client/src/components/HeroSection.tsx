const HeroSection = () => {
  return (
    <div>
      <h1>
        Hero Section(Headline - main message of the site, subheading,
        Call-to-action [Signup, Learn More, Get started])
      </h1>
    </div>
  );
};

export default HeroSection;
